<ui:FluentWindow x:Class="AIVTuber.App.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        xmlns:views="clr-namespace:AIVTuber.App.Views"
        Title="AIVTuber 控制台"
        Height="680" Width="980"
        MinHeight="520" MinWidth="760"
        WindowBackdropType="Mica"
        ExtendsContentIntoTitleBar="True">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <ui:TitleBar Title="AIVTuber 控制台" Grid.Row="0" />

        <ui:NavigationView x:Name="RootNavigation" Grid.Row="1"
                           PaneDisplayMode="Left"
                           IsPaneOpen="True"
                           Loaded="OnNavigationLoaded">
            <ui:NavigationView.MenuItems>
                <ui:NavigationViewItem
                    Content="监控"
                    Icon="{ui:SymbolIcon Symbol=Desktop24}"
                    TargetPageType="{x:Type views:MonitorView}" />
                <ui:NavigationViewItem
                    Content="配置"
                    Icon="{ui:SymbolIcon Symbol=Settings24}"
                    TargetPageType="{x:Type views:ConfigView}" />
                <ui:NavigationViewItem
                    Content="记忆"
                    Icon="{ui:SymbolIcon Symbol=Brain24}"
                    TargetPageType="{x:Type views:MemoryPlaceholderView}" />
            </ui:NavigationView.MenuItems>
        </ui:NavigationView>
    </Grid>
</ui:FluentWindow>
