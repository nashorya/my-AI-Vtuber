<ui:FluentWindow x:Class="AIVTuber.App.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        xmlns:views="clr-namespace:AIVTuber.App.Views"
        Title="AIVTuber 控制台"
        Height="680" Width="980"
        MinHeight="520" MinWidth="760"
        WindowBackdropType="Mica"
        ExtendsContentIntoTitleBar="True">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <ui:TitleBar Title="AIVTuber 控制台" Grid.Row="0" />

        <Border Grid.Row="1"
                MinHeight="{StaticResource CommandBarHeight}"
                Padding="8,4"
                Background="{DynamicResource DeckPanelBrush}"
                BorderBrush="{DynamicResource DeckPanelBorderBrush}"
                BorderThickness="0,0,0,1">
            <ui:Button Content="主题"
                       HorizontalAlignment="Left"
                       Width="64"
                       Appearance="Transparent"
                       ToolTip="切换亮色或深色主题"
                       AutomationProperties.Name="切换亮色或深色主题"
                       Click="OnThemeToggle" />
        </Border>

        <ui:NavigationView x:Name="RootNavigation" Grid.Row="2"
                           PaneDisplayMode="Left"
                           IsPaneOpen="True"
                           Loaded="OnNavigationLoaded">
            <ui:NavigationView.MenuItems>
                <ui:NavigationViewItem
                    Content="监控"
                    Icon="{ui:SymbolIcon Symbol=Desktop24}"
                    TargetPageType="{x:Type views:MonitorView}" />
                <ui:NavigationViewItem
                    Content="记忆"
                    Icon="{ui:SymbolIcon Symbol=Brain24}"
                    TargetPageType="{x:Type views:MemoryView}" />
            </ui:NavigationView.MenuItems>
            <ui:NavigationView.FooterMenuItems>
                <ui:NavigationViewItem
                    Content="配置"
                    Icon="{ui:SymbolIcon Symbol=Settings24}"
                    TargetPageType="{x:Type views:ConfigView}" />
            </ui:NavigationView.FooterMenuItems>
        </ui:NavigationView>
    </Grid>
</ui:FluentWindow>
