<ui:FluentWindow x:Class="AIVTuber.App.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        xmlns:views="clr-namespace:AIVTuber.App.Views"
        Title="AIVTuber 控制台"
        Height="680" Width="980"
        MinHeight="520" MinWidth="760"
        WindowBackdropType="Mica"
        ExtendsContentIntoTitleBar="True">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <ui:TitleBar Title="AIVTuber 控制台" Grid.Row="0" />

        <Grid Grid.Row="1">
            <views:WebConsoleView x:Name="ConsoleHost" />
            <views:FirstRunView x:Name="FirstRunHost" Visibility="Collapsed" />
        </Grid>
    </Grid>
</ui:FluentWindow>
