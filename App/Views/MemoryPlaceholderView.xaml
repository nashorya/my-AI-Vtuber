<UserControl x:Class="AIVTuber.App.Views.MemoryPlaceholderView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml">
    <Grid>
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Opacity="0.5">
            <ui:SymbolIcon Symbol="Brain24" FontSize="56"
                           HorizontalAlignment="Center"
                           Foreground="{DynamicResource TextFillColorTertiaryBrush}" />
            <TextBlock Text="记忆管理" HorizontalAlignment="Center"
                       FontSize="18" FontWeight="Light" Margin="0,16,0,6"
                       Foreground="{DynamicResource TextFillColorSecondaryBrush}" />
            <TextBlock Text="Phase 3 — 即将实现" HorizontalAlignment="Center"
                       FontSize="13"
                       Foreground="{DynamicResource TextFillColorTertiaryBrush}" />
        </StackPanel>
    </Grid>
</UserControl>
