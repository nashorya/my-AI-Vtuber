<UserControl x:Class="AIVTuber.App.Views.ConfigView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="16" MaxWidth="640" HorizontalAlignment="Left">

            <TextBlock Text="LLM 大模型 · 秒生效" FontWeight="Bold" Margin="0,0,0,6" />
            <TextBlock Text="Base URL" Foreground="Gray" />
            <TextBox Text="{Binding Working.Llm.BaseUrl}" Margin="0,2,0,6" />
            <TextBlock Text="Model" Foreground="Gray" />
            <TextBox Text="{Binding Working.Llm.Model}" Margin="0,2,0,6" />
            <TextBlock Text="API Key（留空=不修改）" Foreground="Gray" />
            <PasswordBox x:Name="LlmKey" Margin="0,2,0,6" />
            <TextBlock Text="人设 system_prompt" Foreground="Gray" />
            <TextBox Text="{Binding Working.Llm.SystemPrompt}" AcceptsReturn="True" MinHeight="60"
                     TextWrapping="Wrap" VerticalScrollBarVisibility="Auto" Margin="0,2,0,6" />

            <TextBlock Text="音频 · 保存后重连" FontWeight="Bold" Margin="0,12,0,6" />
            <TextBlock Text="输入设备" Foreground="Gray" />
            <ComboBox ItemsSource="{Binding InputDevices}"
                      SelectedIndex="{Binding Working.Audio.InputDeviceIndex}" Margin="0,2,0,6" />

            <TextBlock Text="TTS 语音合成 · 秒生效" FontWeight="Bold" Margin="0,12,0,6" />
            <TextBlock Text="Provider（如 fish-audio）" Foreground="Gray" />
            <TextBox Text="{Binding Working.Tts.Provider}" Margin="0,2,0,6" />
            <TextBlock Text="API Key（留空=不修改）" Foreground="Gray" />
            <PasswordBox x:Name="TtsKey" Margin="0,2,0,6" />
            <TextBlock Text="Voice ID" Foreground="Gray" />
            <TextBox Text="{Binding Working.Tts.VoiceId}" Margin="0,2,0,6" />

            <TextBlock Text="ASR 语音识别 · 秒生效" FontWeight="Bold" Margin="0,12,0,6" />
            <TextBlock Text="Provider" Foreground="Gray" />
            <TextBox Text="{Binding Working.Asr.Provider}" Margin="0,2,0,6" />
            <TextBlock Text="API Key（留空=不修改）" Foreground="Gray" />
            <PasswordBox x:Name="AsrKey" Margin="0,2,0,6" />

            <TextBlock Text="VTS · 连接改动需重连" FontWeight="Bold" Margin="0,12,0,6" />
            <TextBlock Text="Host" Foreground="Gray" />
            <TextBox Text="{Binding Working.Vts.Host}" Margin="0,2,0,6" />
            <TextBlock Text="Port" Foreground="Gray" />
            <TextBox Text="{Binding Working.Vts.Port}" Margin="0,2,0,6" />

            <TextBlock Text="OBS / B站 · 开关与连接需重连" FontWeight="Bold" Margin="0,12,0,6" />
            <CheckBox Content="启用 OBS 字幕" IsChecked="{Binding Working.Obs.Enable}" Margin="0,2" />
            <CheckBox Content="启用 B站弹幕" IsChecked="{Binding Working.Bilibili.Enable}" Margin="0,2" />

            <TextBlock Text="工具 / 联网搜索（即将支持）" FontWeight="Bold" Foreground="Gray" Margin="0,12,0,6" />
            <CheckBox Content="启用联网搜索" IsEnabled="False" Margin="0,2" />

            <StackPanel Orientation="Horizontal" Margin="0,16,0,0">
                <Button x:Name="SaveButton" Content="保存并应用" Click="OnSave" Padding="12,4" />
                <TextBlock Text="{Binding Status}" VerticalAlignment="Center" Margin="12,0,0,0" Foreground="Gray" />
            </StackPanel>

        </StackPanel>
    </ScrollViewer>
</UserControl>
