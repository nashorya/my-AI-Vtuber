<Project>
  <!-- Single version source for assemblies, UI, diagnostics and package names. -->
  <PropertyGroup>
    <VersionPrefix>0.36.0</VersionPrefix>
    <!-- Release candidate label; drop it (or bump) when cutting a release. -->
    <VersionSuffix>rc.2</VersionSuffix>
  </PropertyGroup>
</Project>
